import { ConnectFormDirectiveDirective } from './connect-form-directive.directive';

describe('ConnectFormDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectFormDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
